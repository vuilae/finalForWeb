<head>
  <style>
    .form {
  max-width: 600px;
  margin: 20px auto;
  padding: 20px;
  background-color: rgba(0, 0, 0, 0.5);
  border-radius: 8px;
  box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
  text-align: center;
  color: #f8f8ff;
}

.input,
.select {
  width: 100%;
  padding: 10px;
  margin-bottom: 16px;
  box-sizing: border-box;
  border-radius: 7px;
}
  </style>
  <link rel="stylesheet" href="/styles/nav.css" />
  <link
    href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css"
    rel="stylesheet"
  />
  <script
    src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/js/bootstrap.bundle.min.js"
    integrity="sha384-MrcW6ZMFYlzcLA8Nl+NtUVF0sA7MsXsP1UyJoMp4YLEuNSfAP+JcXn/tWtIaxVXM"
    crossorigin="anonymous"
  ></script>
</head>
<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <div class="container">
    <a class="navbar-brand" href="/tours?lang=<%= currentLanguage %>">
      <img src="/images/logo.png" width="60px" alt="Darina" />
    </a>
    <div class="ms-auto">
      <button
        class="navbar-toggler"
        type="button"
        data-bs-toggle="collapse"
        data-bs-target="#navbarNav"
        aria-controls="navbarNav"
        aria-expanded="false"
        aria-label="Toggle navigation"
      >
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav">
          <li class="nav-item">
            <a class="nav-link a-tag" href="/tours?lang=<%= currentLanguage %>"
              ><%= translation.navTour %></a
            >
          </li>
          <li class="nav-item">
            <a class="nav-link a-tag" href="/destinations?lang=<%= currentLanguage %>"
              ><%= translation.navHotel %></a
            >
          </li>
          <li class="nav-item">
            <a
              class="nav-link a-tag"
              href="/airlines?lang=<%= currentLanguage %>"
              ><%= translation.navAirline %></a
            >
          </li>
          <li class="nav-item">
            <a class="nav-link a-tag" href="/booked?lang=<%= currentLanguage %>"
              ><%= translation.navBooked %></a
            >
          </li>
          <% if (admin) { %>
          <li class="nav-item">
            <a class="nav-link a-tag" href="/admin?lang=<%= currentLanguage %>"
              ><%= translation.navAdmin %></a
            >
          </li>
          <% } %>
          <li class="nav-item">
            <a class="nav-link a-tag" href="/login?lang=<%= currentLanguage %>"
              ><%= translation.navAuth %></a
            >
          </li>
          <li class="nav-item dropdown">
            <form class='f0rm form bg-white d-flex align-items-center justify-content-center' id="language-form" action="/change-language" method="POST" style="width: 120px; height: 20px; color:black">
              <%= translation.navLang %>:
                <select class="select" name="language" onchange="this.form.submit()" style="width: calc(100% - 40px); height: 100%; border: none; margin-left: 10px;text-align: center;">
                    <option value="en" <% if(currentLanguage === 'en') { %> selected <% } %>>En</option>
                    <option value="kz" <% if(currentLanguage === 'kz') { %> selected <% } %>>Kz</option>
                </select>
            </form>
        </li>      
        </ul>
      </div>
    </div>
  </div>
</nav>
